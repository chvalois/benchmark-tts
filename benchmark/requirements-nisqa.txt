# NISQA-TTS — naturalité par énoncé (contre-vérification secondaire).
# Le paquet `nisqa` épingle torch 2.2 / numpy 1.26 → venv DÉDIÉ `_nisqa`.
#
#   source env.sh
#   uv venv "$TTSB_VENVS/_nisqa" --python 3.11
#   uv pip install --python "$TTSB_VENVS/_nisqa/bin/python" \
#       -r benchmark/requirements-nisqa.txt
#
# Poids (hors PyPI, CC BY-NC-SA 4.0 — usage recherche non commercial) :
#   git clone --depth 1 https://github.com/gabrielmittag/NISQA \
#       "$TTSB_ROOT/nisqa-weights"
#   # -> $TTSB_ROOT/nisqa-weights/weights/nisqa_tts.tar

nisqa>=2.0
librosa>=0.10
soundfile>=0.12
pandas>=2.0
pyyaml>=6.0
setuptools<81   # librosa 0.10 importe `pkg_resources` (retiré de setuptools>=81)

# Environnement local (copie de env.sh.example, contient des chemins machine)
env.sh

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.venv/
venv/

# Sorties volumineuses — vivent sur D: ($TTSB_AUDIO_OUT), pas dans le repo
/audio_genere/*
!/audio_genere/.gitkeep
/resultats/*.csv
/resultats/*.json
!/resultats/.gitkeep

# Poids / venvs par modèle : jamais commités (voir models.lock + fetch_models.py)
/models/*/.venv/
/models/*/weights/

# Voix de référence : données vocales personnelles (consentement/licence
# individuels — cf. corpus/voix_reference/README.md). Jamais poussées.
corpus/voix_reference/*.wav
corpus/voix_reference/*.flac
corpus/voix_reference/*.mp3
